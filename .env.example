# ── 서버 전용 (API 라우트에서만 읽음, 브라우저 번들에 안 들어감) ──
ANTHROPIC_API_KEY=

# ── Supabase (대시보드 → Project Settings → API) ──
# URL과 publishable(anon) 키는 공개돼도 되는 값. 데이터는 RLS로 보호된다
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=

# 네이티브 앱에서 쓸 배포 서버 주소 (웹은 비워두면 같은 도메인 사용)
EXPO_PUBLIC_API_URL=
